---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>Marc Evangelista - Business Card</title>
		
		<!-- Windows CSS libraries -->
		<link rel="stylesheet" href="https://unpkg.com/98.css" id="win98-css" />
		<link rel="stylesheet" href="https://unpkg.com/xp.css" id="winxp-css" disabled />
		
		<style>
			@page {
				size: 3.5in 2in;
				margin: 0;
			}
			
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}
			
			body {
				margin: 0;
				padding: 0;
			}
			
			.card-container {
				width: 3.5in;
				height: 2in;
				position: relative;
				overflow: visible;
				padding: 2px;
			}
			
			.window {
				width: calc(100% - 4px);
				height: calc(100% - 4px);
				margin: 0;
				display: flex;
				flex-direction: column;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.title-bar {
				flex-shrink: 0;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.title-bar-text {
				font-size: 11px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.title-bar-controls button {
				min-width: 16px;
				min-height: 14px;
				padding: 0;
			}
			
			.window-body {
				flex-grow: 1;
				padding: 8px;
				display: flex;
				flex-direction: column;
				justify-content: space-between;
				font-size: 10px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.content-top {
				flex-grow: 1;
			}
			
			.name {
				font-size: 14pt;
				font-weight: bold;
				margin-bottom: 4px;
				font-family: 'Arial', 'Tahoma', sans-serif !important;
			}
			
			.tagline {
				font-size: 8pt;
				color: #666;
				margin-bottom: 8px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.achievement {
				font-size: 16pt;
				font-weight: bold;
				color: #0066cc;
				margin-bottom: 2px;
				font-family: 'Arial', 'Tahoma', sans-serif !important;
			}
			
			.achievement-detail {
				font-size: 8pt;
				margin-bottom: 6px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.skills {
				font-size: 7pt;
				margin-bottom: 6px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			.contact {
				display: flex;
				justify-content: space-between;
				font-size: 8pt;
				border-top: 1px solid #ccc;
				padding-top: 6px;
				font-family: 'Tahoma', 'Arial', sans-serif !important;
			}
			
			/* Sticky note accent */
			.sticky-accent {
				position: absolute;
				top: 30px;
				right: 8px;
				width: 50px;
				height: 50px;
				background: #FFFD75;
				transform: rotate(-5deg);
				box-shadow: 1px 1px 3px rgba(0,0,0,0.2);
				padding: 6px;
				font-size: 6pt;
				font-family: 'Comic Sans MS', cursive;
				z-index: 10;
			}
			
			/* Adjust for XP theme */
			.winxp .window {
				border-radius: 6px 6px 0 0;
			}
			
			.winxp .title-bar {
				border-radius: 6px 6px 0 0;
			}
			
			/* Override 98.css pixelated fonts */
			.window,
			.window * {
				font-smooth: always;
				-webkit-font-smoothing: antialiased;
				-moz-osx-font-smoothing: grayscale;
			}
			
			/* Force non-pixelated fonts */
			body, .window, .title-bar, .window-body {
				image-rendering: auto !important;
			}
			
			/* Print styles */
			@media print {
				body {
					margin: 0;
					padding: 0;
					background: white;
				}
				
				.card-container {
					page-break-after: always;
					overflow: hidden;
				}
			}
			
			/* Screen preview styles */
			@media screen {
				body {
					background: #008080;
					padding: 20px;
					display: flex;
					justify-content: center;
					align-items: center;
					min-height: 100vh;
				}
				
				.card-container {
					background: white;
					box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
				}
			}
			
			/* Theme switcher for preview */
			.theme-controls {
				position: fixed;
				top: 10px;
				right: 10px;
				background: white;
				padding: 10px;
				border: 2px solid #000;
				z-index: 100;
			}
			
			@media print {
				.theme-controls {
					display: none;
				}
			}
		</style>
	</head>
	<body>
		<!-- Theme controls for preview only -->
		<div class="theme-controls">
			<label>
				<input type="radio" name="theme" value="98" checked> Win98
			</label>
			<label>
				<input type="radio" name="theme" value="xp"> WinXP
			</label>
		</div>
		
		<div class="card-container">
			<div class="window">
				<div class="title-bar">
					<div class="title-bar-text">Marc Evangelista - real human engineer</div>
					<div class="title-bar-controls">
						<button aria-label="Minimize"></button>
						<button aria-label="Maximize"></button>
						<button aria-label="Close"></button>
					</div>
				</div>
				<div class="window-body">
					<div class="sticky-accent">
						TODO:<br>
						☐ 60x<br>
						☐ AI<br>
						☐ humor
					</div>
					
					<div class="content-top">
						<div class="name">Marc Evangelista</div>
						<div class="tagline">AI engineer. make computer do work.</div>
						
						<div class="achievement">60x faster</div>
						<div class="achievement-detail">C++ builds at Bloomberg</div>
						
						<div class="skills">
							Bleeding edge AI agentic workflows • Prompt engineering<br>
							Real 90s baby who grew up with Windows
						</div>
					</div>
					
					<div class="contact">
						<span>marc@evangelista.moe</span>
						<span>github.com/cybermelons</span>
					</div>
				</div>
			</div>
		</div>
		
		<script>
			// Theme switcher for preview
			const themeInputs = document.querySelectorAll('input[name="theme"]');
			const win98CSS = document.getElementById('win98-css');
			const winxpCSS = document.getElementById('winxp-css');
			const container = document.querySelector('.card-container');
			
			themeInputs.forEach(input => {
				input.addEventListener('change', (e) => {
					if (e.target.value === '98') {
						win98CSS.disabled = false;
						winxpCSS.disabled = true;
						container.classList.remove('winxp');
					} else {
						win98CSS.disabled = true;
						winxpCSS.disabled = false;
						container.classList.add('winxp');
					}
				});
			});
		</script>
	</body>
</html>